---
import Example from '../examples/Human.tsx'
import code from '../examples/Human.tsx?raw'
import { pages } from '../examples.ts'
import Layout from '../layouts/ExampleLayout.astro'
import Section from '../layouts/ExampleSection.astro'

const page = pages.find(p => p.slug === 'human')!
---

<Layout page={page}>
  <Section slug="human" code={code}>
    <Example client:only="react" />
  </Section>
</Layout>
