import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import {
  ExternalNavigateLocstring,
  ExternalNavigateObject,
  HorizontallyFlippedViaButton,
  HorizontallyFlippedViaDisplayedRegions,
  HorizontallyFlippedViaLocstring,
  WithMultipleDisplayedRegionsFlipped,
} from './JBrowseLinearGenomeView.stories'

<Meta title="Navigation" />

# Navigation

`createViewState` returns an object whose `.session.view` is the live
`LinearGenomeViewModel`. Components outside the view tree can read it, mutate
it, and call its actions — which is how you wire up "jump to this gene" buttons,
search-result lists, or programmatic tours. This page covers moving the view to
a location and rendering regions in reverse orientation.

## `navToLocString` (string locations)

The two actions you'll reach for most often are `navToLocString` (string form)
and `navToLocations` (parsed-object form). Both are async and will re-set the
displayed regions if needed; both reject on invalid input so you can surface
errors in a banner.

`navToLocString` takes a JBrowse-style locstring like `ctgA:1-5,000` or
`chr1:1m-2m`. Easiest to drive from a `<select>` or a static list of bookmarks:

```js
state.session.view
  .navToLocString('ctgA:1,000..5,000')
  .catch(e => console.error(e))
```

<Canvas of={ExternalNavigateLocstring} />

## `navToLocations` (object locations)

Takes an array of `{ refName, start, end, assemblyName? }` objects. Skip the
string formatting and parsing round-trip when you already have numeric
coordinates — e.g. when bridging from a backend search service. Pass multiple
regions to land in a multi-region view:

```js
state.session.view
  .navToLocations([{ refName: 'ctgA', start: 1050, end: 9000 }])
  .catch(e => console.error(e))
```

<Canvas of={ExternalNavigateObject} />

### `navTo` vs `navToLocations`

There's also a lower-level `navTo(query)` action. The difference: `navTo` only
navigates if the target lies **inside the currently displayed regions** — it
won't re-set them. `navToLocations` (and `navToLocString`) will re-set displayed
regions as needed, so they're almost always what you want for external
navigation.

## Horizontal flipping

Regions can be rendered reversed (3'→5'), which is essential for viewing genes
on the negative strand or for laying out synteny-style comparisons. The view
supports flipping a single locstring, an entire view, or individual regions
within a multi-region view.

### Flip via locstring

Append `[rev]` to a locstring passed to `navToLocString`. The flip happens as
part of navigation.

<Canvas of={HorizontallyFlippedViaLocstring} />

### Flip via button

Toggle the orientation imperatively — useful for custom toolbar buttons or
keyboard shortcuts.

<Canvas of={HorizontallyFlippedViaButton} />

### Flip via displayed regions

Set `reversed: true` on a `displayedRegions` entry to flip just that region.
Other regions in the same view stay in their natural orientation.

<Canvas of={HorizontallyFlippedViaDisplayedRegions} />

### Mixing orientations

Combine forward and reversed regions in a single view — the building block for
gene-centric layouts (always-5'-to-3') and synteny ribbons.

<Canvas of={WithMultipleDisplayedRegionsFlipped} />
