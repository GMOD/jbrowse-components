import { Meta, Story } from '@storybook/addon-docs/blocks'
import { ShadowDOMOneLinearGenomeView } from './JBrowseLinearGenomeView.stories'

<Meta title="Using Shadow DOM custom element" />

# Shadow DOM/custom element usage

This is an example of using the @jbrowse/react-linear-genome-view2 component in
a Shadow DOM context.

<Story of={ShadowDOMOneLinearGenomeView} />
