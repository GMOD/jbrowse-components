import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'

<Meta title="Default Tracks" />

# Default tracks

Using an array defaultTracks, you can specify a simple array of trackIds. This
can sometimes by simpler than the method of specifying the defaultTracks using
the defaultSession

This example has a simple BigWig track shown by default

<Canvas withSource="open">
  <Story id="linear-view--using-show-tracks" />
</Canvas>
