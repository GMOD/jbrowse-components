import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import { WithInit } from './JBrowseLinearGenomeView.stories'

<Meta title="Getting Started" />

# Welcome

This is the [Storybook](https://storybook.js.org/) for the
[JBrowse 2 React Linear Genome View](https://www.npmjs.com/package/@jbrowse/react-linear-genome-view2),
which embeds an interactive genome browser in any React application.

Stories are fully interactive — pan, zoom, and open tracks as you would in your
own app. Click **Show code** below any story to see its full source.

## Recommended setup: `init`

The recommended way to embed JBrowse is via the `init` field on
`defaultSession.view`. You give it an assembly name, a starting locstring, and
the trackIds you want open on first paint — and JBrowse fills in the rest.

```js
const state = createViewState({
  assembly,
  tracks,
  defaultSession: {
    name: 'My session',
    view: {
      type: 'LinearGenomeView',
      init: {
        assembly: 'hg38',
        loc: 'chr1:11,106,077-11,261,675',
        tracks: ['ncbi-refseq-genes'],
      },
    },
  },
})
```

The story below is exactly this pattern, wired up to GRCh38 with a real RefSeq
gene track:

<Canvas of={WithInit} />

`init.loc` accepts any locstring, including space-separated multi-region strings
(e.g. `'chr1:100-200 chr1:500-600'`). `init.tracks` is a plain list of trackIds
to open.

## Where to next

- [Initialization](?path=/docs/initialization--docs) — other ways to specify the
  initial location, opening tracks programmatically, default sessions, and the
  verbose snapshot form for round-tripping exported sessions
- [Navigation](?path=/docs/navigation--docs) — driving navigation from buttons
  or search results outside the view, and flipping region orientation
- [Observing View State](?path=/docs/observing-view-state--docs) — build
  companion panels that react to the view via MobX observers
- [Styling and Embedding](?path=/docs/styling-and-embedding--docs) — theming,
  light/dark mode, locking down the track list, the side drawer, and Shadow DOM
- [Web Worker RPC](?path=/docs/web-worker-rpc--docs) — move parsing and
  rendering off the main thread for large alignments
- [Source for every storybook example](https://github.com/GMOD/jbrowse-components/tree/main/products/jbrowse-react-linear-genome-view/stories)
- Integration recipes for create-react-app, Vite, and Next.js —
  [jbrowse.org/jb2/docs/embedded_components](https://jbrowse.org/jb2/docs/embedded_components/)
