import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import {
  WithErrorHandler,
  WithTwoLinearGenomeViews,
} from './JBrowseLinearGenomeView.stories'

<Meta title="Recipes" />

# Recipes

Smaller integration patterns that don't need a page of their own.

## Multiple linear genome views

Two or more `JBrowseLinearGenomeView` instances can coexist on a single page —
each gets its own `viewState` from `createViewState`, and they share no
navigation, tracks, or session state. Useful for side-by-side locus comparisons,
multi-assembly dashboards, or report-style layouts with several small embedded
views.

<Canvas of={WithTwoLinearGenomeViews} />

If you need the two views to track each other (linked panning, shared zoom),
wire it up yourself via the MobX state tree — wrap your sibling component in
`observer`, read one view's `bpPerPx`/`offsetPx`, and call the matching actions
on the other. Both `viewState` objects are ordinary state-tree nodes, so
anything one view exposes can be mirrored onto another. The
[LinearGenomeView state model docs](https://jbrowse.org/jb2/docs/models/lineargenomeview/)
list every property to read and action to call when wiring this up.

## Error handling

The embedded view does not ship with a built-in error boundary, so catching
errors is the host application's responsibility. There are two classes of error
to handle:

**Construction errors** — `createViewState` validates the configuration and can
throw a (sometimes verbose) `@jbrowse/mobx-state-tree` error. Wrap the call in
try/catch in your component.

**Runtime errors** — observable on `viewState.session.view.error`. An observer
component can render an error UI when this becomes truthy.

For both cases we recommend the
[`@jbrowse/core/ui/ErrorMessage`](https://github.com/GMOD/jbrowse-components/blob/main/packages/core/ui/ErrorMessage.tsx)
component (note: an error _display_, not a React error boundary) — it formats
JBrowse errors with helpful context.

The example below configures a `BadTrack` track type that fails validation and
renders the resulting error in a red banner.

<Canvas of={WithErrorHandler} />
