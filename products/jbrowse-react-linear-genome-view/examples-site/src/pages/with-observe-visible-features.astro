---
import Example from '../examples/WithObserveVisibleFeatures.tsx'
import code from '../examples/WithObserveVisibleFeatures.tsx?raw'
import { examples } from '../examples.ts'
import Layout from '../layouts/ExampleLayout.astro'

const meta = examples.find(e => e.slug === 'with-observe-visible-features')!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
