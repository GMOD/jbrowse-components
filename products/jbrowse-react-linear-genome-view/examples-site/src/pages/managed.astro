---
import Layout from '../layouts/ExampleLayout.astro'
import Example from '../examples/Managed.tsx'
import code from '../examples/Managed.tsx?raw'
import { examples } from '../examples.ts'

const meta = examples.find(e => e.slug === 'managed')!
---

<Layout title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
