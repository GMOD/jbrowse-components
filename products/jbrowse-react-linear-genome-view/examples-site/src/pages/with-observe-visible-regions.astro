---
import Example from '../examples/WithObserveVisibleRegions.tsx'
import code from '../examples/WithObserveVisibleRegions.tsx?raw'
import { examples } from '../examples.ts'
import Layout from '../layouts/ExampleLayout.astro'

const meta = examples.find(e => e.slug === 'with-observe-visible-regions')!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
