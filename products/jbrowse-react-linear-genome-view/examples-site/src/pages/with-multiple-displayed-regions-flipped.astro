---
import Layout from '../layouts/ExampleLayout.astro'
import Example from '../examples/WithMultipleDisplayedRegionsFlipped.tsx'
import code from '../examples/WithMultipleDisplayedRegionsFlipped.tsx?raw'
import { examples } from '../examples.ts'

const meta = examples.find(e => e.slug === 'with-multiple-displayed-regions-flipped')!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
