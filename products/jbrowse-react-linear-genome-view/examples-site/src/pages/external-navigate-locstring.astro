---
import Layout from '../layouts/ExampleLayout.astro'
import Example from '../examples/ExternalNavigateLocstring.tsx'
import code from '../examples/ExternalNavigateLocstring.tsx?raw'
import { examples } from '../examples.ts'

const meta = examples.find(e => e.slug === 'external-navigate-locstring')!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
