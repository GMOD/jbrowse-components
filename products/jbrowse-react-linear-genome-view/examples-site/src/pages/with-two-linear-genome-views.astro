---
import Example from '../examples/WithTwoLinearGenomeViews.tsx'
import code from '../examples/WithTwoLinearGenomeViews.tsx?raw'
import { examples } from '../examples.ts'
import Layout from '../layouts/ExampleLayout.astro'

const meta = examples.find(e => e.slug === 'with-two-linear-genome-views')!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
