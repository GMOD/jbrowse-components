import { Meta, Story } from '@storybook/addon-docs/blocks'
import {
  WithImportConfigJson,
  WithFetchConfigJson,
} from './JBrowseReactApp.stories'

<Meta title="Importing config.json files" />

# Importing config.json files

The jbrowse-web app fetches a config.json from the current directory (or
wherever the `?config=` URL parameter points). `@jbrowse/react-app2` makes no
assumptions about URL parameters, so you must supply the config yourself.

## Using static JSON imports

<Story of={WithImportConfigJson} />

## Using `fetch`

<Story of={WithFetchConfigJson} />
