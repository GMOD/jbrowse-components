import { Canvas, Meta } from '@storybook/addon-docs/blocks'
import {
  BreakpointSplitExample,
  CircularExample,
  DotplotExample,
  SpreadsheetExample,
  SvInspectorExample,
  SyntenyExample,
  WithLaunchLinearGenomeView,
} from './JBrowseReactApp.stories'

<Meta title="View Types" />

# View types

One of the things `react-app2` gives you that the standalone LGV component does
not is a **session that can hold any number of views of any type**, side by side
— `LinearGenomeView`, `LinearSyntenyView`, `DotplotView`, `CircularView`,
`SpreadsheetView`, and more.

Each view type launches the same declarative way: a `defaultSession.views` entry
with a `type` and an `init` field. `init` is the same shape JBrowse Web
serializes into its `?session=spec-…` URL parameter (see the
[URL query parameter API](https://jbrowse.org/jb2/docs/urlparams/)) — so these
examples are the programmatic equivalent of those URLs. The exact fields each
view's `init`/snapshot accepts come from its
[state model docs](https://jbrowse.org/jb2/docs/models/).

## Synteny: two assemblies in one session

`LinearSyntenyView` shows two linear genome views with a connecting ribbon for
synteny features (PAF, MUMMER, etc.). The `init` field declares the two member
assemblies and the synteny track that ties them together — much terser than
hand-building two `LinearGenomeView` snapshots plus a synteny view snapshot:

```js
{
  id: 'synteny_view',
  type: 'LinearSyntenyView',
  init: {
    views: [{ assembly: 'volvox' }, { assembly: 'volvox_del' }],
    tracks: ['volvox_del.paf'],
  },
}
```

<Canvas of={SyntenyExample} />

## Dotplot

`DotplotView` compares two assemblies as a 2D dotplot. `init.views` lists the
two assemblies; `tracks` lists the synteny tracks. (Self-vs-self is allowed.)

```js
{
  id: 'dotplot_view',
  type: 'DotplotView',
  init: {
    views: [{ assembly: 'volvox' }, { assembly: 'volvox' }],
    tracks: ['volvox_fake_synteny'],
  },
}
```

<Canvas of={DotplotExample} />

## Circular

`CircularView` renders structural variants as arcs around a circular ideogram.
`init` takes a single `assembly` and the `tracks` to show.

```js
{
  id: 'circular_view',
  type: 'CircularView',
  init: {
    assembly: 'volvox',
    tracks: ['volvox_sv_test'],
  },
}
```

<Canvas of={CircularExample} />

## Spreadsheet

`SpreadsheetView` opens a tabular file (e.g. a VCF) as a sortable, filterable
spreadsheet. `init` takes the `assembly` and a `uri` to the file — note `uri` is
on `init`, not a track config.

```js
{
  id: 'spreadsheet_view',
  type: 'SpreadsheetView',
  init: {
    assembly: 'volvox',
    uri: 'test_data/volvox/volvox.filtered.vcf.gz',
  },
}
```

<Canvas of={SpreadsheetExample} />

## SV inspector

`SvInspectorView` pairs a spreadsheet of structural variants with a circular
view, so clicking a row highlights the variant's breakend loci in the circular
view. Same `init` shape as the spreadsheet view.

```js
{
  id: 'sv_inspector_view',
  type: 'SvInspectorView',
  init: {
    assembly: 'volvox',
    uri: 'test_data/volvox/volvox.dup.vcf.gz',
  },
}
```

<Canvas of={SvInspectorExample} />

## Breakpoint split view

`BreakpointSplitView` stacks two (or more) linear genome views and draws curves
between the breakpoints of split/translocated reads. `init.views` is an array of
`{ loc, assembly, tracks }` — one per stacked panel.

```js
{
  id: 'breakpoint_split_view',
  type: 'BreakpointSplitView',
  init: {
    views: [
      { loc: 'ctgA:1-5000', assembly: 'volvox', tracks: ['volvox_sv_cram'] },
      { loc: 'ctgB:1-5000', assembly: 'volvox', tracks: ['volvox_sv_cram'] },
    ],
  },
}
```

<Canvas of={BreakpointSplitExample} />

## Launching a view programmatically

For views that should appear in response to runtime conditions (a button click,
a search hit, a backend event) rather than at first paint, use the
`LaunchView-*` extension points instead of baking the view into
`defaultSession`. The example below boots an empty session and then launches a
`LinearGenomeView` after mount:

<Canvas of={WithLaunchLinearGenomeView} />

This is the same machinery the import wizard uses internally, so anything the
user can launch from the UI you can launch from code. See the
[API Reference](?path=/docs/api-reference--docs) for the full list of
`LaunchView-*` extension points.
