import { Meta, Story } from '@storybook/addon-docs/blocks'
import { WithWebWorker } from './JBrowseReactApp.stories'

<Meta title="Using WebWorker RPC" />

# Web Worker RPC

Using WebWorker RPC reduces main-thread blocking when browsing BAM/CRAM data.
Pass a `makeWorkerInstance` factory to `createViewState`:

<Story of={WithWebWorker} />

If workers don't resolve correctly in webpack, set `"publicPath":"auto"` in your
webpack config
[(see this issue)](https://github.com/webpack/webpack/issues/13791#issuecomment-897579223).
