---
import Layout from "../layouts/ExampleLayout.astro"
import Example from "../examples/WithLaunchLinearGenomeView.tsx"
import code from "../examples/WithLaunchLinearGenomeView.tsx?raw"
import { examples } from "../examples.ts"

const meta = examples.find(e => e.slug === "with-launch-linear-genome-view")!
---

<Layout slug={meta.slug} title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
