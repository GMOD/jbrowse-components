---
import Layout from "../layouts/ExampleLayout.astro"
import Example from "../examples/WithFetchConfigJson.tsx"
import code from "../examples/WithFetchConfigJson.tsx?raw"
import { examples } from "../examples.ts"

const meta = examples.find(e => e.slug === "with-fetch-config-json")!
---

<Layout title={meta.title} description={meta.description} code={code}>
  <Example client:only="react" />
</Layout>
