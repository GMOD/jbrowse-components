`@jbrowse/react-app2` embeds the **full JBrowse 2 application** — menu bar, view
manager, drawer widgets, and every view type — inside a React component tree. If
you only need a single linear track view without the app chrome, the lighter
[`@jbrowse/react-linear-genome-view2`](https://jbrowse.org/storybook/lgv/)
component is a better fit.

Build the model with `createViewState`, then render `<JBrowseApp>`:

```jsx
import { JBrowseApp, createViewState } from '@jbrowse/react-app2'

const state = createViewState({
  config: {
    assemblies: [
      {
        name: 'volvox',
        sequence: {/* ... */},
      },
    ],
    tracks: [{ trackId: 'my_track', type: 'AlignmentsTrack' /* ... */ }],
    defaultSession: {
      views: [
        {
          type: 'LinearGenomeView',
          init: {
            assembly: 'volvox',
            loc: 'ctgA:1..50000',
            tracks: ['my_track'],
            tracklist: true, // open the track selector on load
          },
        },
      ],
    },
  },
})

function App() {
  return <JBrowseApp viewState={state} />
}
```

The `config` object uses the same format as a JBrowse Web `config.json`, so
configs round-trip between the two. Unlike JBrowse Web, the embedded component
does **not** auto-fetch a `config.json` from a URL parameter — you control how
and when the config is loaded (see
[Import a config.json](../with-import-config-json/)).

`createViewState` also accepts:

- `plugins` — inline or imported [plugin](../embedded-plugin/) classes
- `makeWorkerInstance` — a [web worker](../with-web-worker/) factory for the
  WebWorker RPC
- `onChange` — an [observer](../with-on-change/) called on every MST patch

Every config field is autogenerated under
[docs/config](https://jbrowse.org/jb2/docs/config/) (e.g.
[JBrowseRootConfig](https://jbrowse.org/jb2/docs/config/jbrowserootconfig/),
[FeatureTrack](https://jbrowse.org/jb2/docs/config/featuretrack/)) and view
snapshot/`init` fields under [docs/models](https://jbrowse.org/jb2/docs/models/)
(e.g.
[LinearGenomeView](https://jbrowse.org/jb2/docs/models/lineargenomeview/)).
Integration recipes for create-react-app, Vite, and Next.js live at
[jbrowse.org/jb2/docs/embedded_components](https://jbrowse.org/jb2/docs/embedded_components/).
