[Supported file types](https://jbrowse.org/jb2/docs/config_guides/file_types/)
lists what each extension resolves to. `assemblyNames` is implied only while the
config declares one assembly. Spell `type` and `adapter` out when the file name
does not decide the format, or to set an adapter slot such as `csi: true`.
