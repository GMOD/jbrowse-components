`SvInspectorView` pairs a spreadsheet of structural variants with a circular
view, so clicking a row highlights that variant's breakend loci. Same `init`
shape as the [spreadsheet view](#spreadsheet-example).

Fields: [SvInspectorView](https://jbrowse.org/jb2/docs/models/svinspectorview/).
