// Generated by scripts/generateMarkRules.ts from plugins/marks/src/LinearMarkDisplay/shapeLanes.ts: do not edit, `pnpm autogen` rewrites it.
import type { MarkShapeName } from './markVocabulary.ts'

/**
 * The lanes the worker fills for each shape. The encoder fills whichever of
 * `color` and `colorValue` the colour declaration calls for.
 *
 * Its own module, reaching only the vocabulary beside it: the rule list reads
 * it and `jbrowse validate` carries a copy, and markList.ts takes render-core's
 * marks barrel — shader sources included — which the examples sites' eager
 * budget then pays for. `layerRequests` in model.ts is where each lane is
 * checked to be one the encoder fills.
 */
export const SHAPE_LANES = {
  bar: ['y', 'row', 'color', 'colorValue', 'index'],
  point: ['y', 'row', 'color', 'colorValue', 'glyph', 'index'],
  span: ['row', 'color', 'index'],
} as const satisfies Record<MarkShapeName, readonly string[]>
