# Standalone npm project; npm ci requires the lockfile committed
# (root .gitignore ignores **/package-lock.json for the pnpm monorepo)
!package-lock.json

# Regenerable `sam build` output; never commit
.aws-sam/
