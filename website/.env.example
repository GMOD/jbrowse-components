# Newsletter API URL — output from `sam deploy` (Outputs.ApiUrl)
# Leave unset to hide the newsletter signup form entirely.
PUBLIC_NEWSLETTER_API_URL=https://XXXXXXXXXX.execute-api.us-east-1.amazonaws.com
