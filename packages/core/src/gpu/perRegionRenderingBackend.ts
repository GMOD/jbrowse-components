// Re-export shim: this module moved to @jbrowse/render-core.
// Kept so existing `@jbrowse/core/gpu/perRegionRenderingBackend` imports keep resolving.
export * from '@jbrowse/render-core/perRegionRenderingBackend'
