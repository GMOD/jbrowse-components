// Re-export shim: this module moved to @jbrowse/render-core.
// Kept so existing `@jbrowse/core/gpu/globalRenderingBackend` imports keep resolving.
export * from '@jbrowse/render-core/globalRenderingBackend'
