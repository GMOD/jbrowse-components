// Re-export shim: this module moved to @jbrowse/render-core.
// Kept so existing `@jbrowse/core/gpu/RenderLifecycleMixin` imports keep resolving.
export * from '@jbrowse/render-core/RenderLifecycleMixin'
