# packages/core

Don't hand-edit the `exports`/`publishConfig` maps in `package.json` — run `pnpm generate:exports` (scans actual `@jbrowse/core/*` imports) after adding/removing an importable module.
