// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from coverageMod.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

import type { VertexAttributeLayout, ShaderBinding } from '@jbrowse/render-core/hal'

export const BINDINGS: readonly ShaderBinding[] = [
  { index: 1, kind: 'uniform', name: 'u' },
]

export const VERTS_PER_INSTANCE = 6

export const UNIFORMS_SIZE_BYTES = 112

// Word indices into a Float32Array view over the uniform buffer.
export const UNIFORM_OFFSET_F32 = {
  bpHi: 0,
  bpLo: 1,
  bpLen: 2,
  hpZero: 3,
  canvasW: 4,
  canvasH: 5,
  covHeight: 6,
  covYOffset: 7,
  covTop: 8,
  regionMaxDepth: 9,
  depthDomainMax: 10,
  depthDomainMin: 11,
  coverageSymlogConstant: 12,
  binSize: 13,
  interbaseHeight: 14,
  snpMinFreq: 15,
  reversed: 16,
} as const

// Word indices into a Uint32Array view over the uniform buffer.
export const UNIFORM_OFFSET_U32 = {
  colorCoverage: 18,
  colorBaseA: 19,
  colorBaseC: 20,
  colorBaseG: 21,
  colorBaseT: 22,
  colorBaseN: 23,
  colorInsertionIndicator: 24,
  colorSoftclipIndicator: 25,
  colorHardclipIndicator: 26,
} as const

// Word indices into a Int32Array view over the uniform buffer.
export const UNIFORM_OFFSET_I32 = {
  coverageScaleType: 17,
} as const


export interface Uniforms {
  bpHi: number
  bpLo: number
  bpLen: number
  hpZero: number
  canvasW: number
  canvasH: number
  covHeight: number
  covYOffset: number
  covTop: number
  regionMaxDepth: number
  depthDomainMax: number
  depthDomainMin: number
  coverageSymlogConstant: number
  binSize: number
  interbaseHeight: number
  snpMinFreq: number
  reversed: number
  coverageScaleType: number
  colorCoverage: number
  colorBaseA: number
  colorBaseC: number
  colorBaseG: number
  colorBaseT: number
  colorBaseN: number
  colorInsertionIndicator: number
  colorSoftclipIndicator: number
  colorHardclipIndicator: number
}

export function writeUniforms(buf: ArrayBuffer, uniforms: Uniforms) {
  const f32 = new Float32Array(buf)
  const u32 = new Uint32Array(buf)
  const i32 = new Int32Array(buf)
  f32[0] = uniforms.bpHi
  f32[1] = uniforms.bpLo
  f32[2] = uniforms.bpLen
  f32[3] = uniforms.hpZero
  f32[4] = uniforms.canvasW
  f32[5] = uniforms.canvasH
  f32[6] = uniforms.covHeight
  f32[7] = uniforms.covYOffset
  f32[8] = uniforms.covTop
  f32[9] = uniforms.regionMaxDepth
  f32[10] = uniforms.depthDomainMax
  f32[11] = uniforms.depthDomainMin
  f32[12] = uniforms.coverageSymlogConstant
  f32[13] = uniforms.binSize
  f32[14] = uniforms.interbaseHeight
  f32[15] = uniforms.snpMinFreq
  f32[16] = uniforms.reversed
  i32[17] = uniforms.coverageScaleType
  u32[18] = uniforms.colorCoverage
  u32[19] = uniforms.colorBaseA
  u32[20] = uniforms.colorBaseC
  u32[21] = uniforms.colorBaseG
  u32[22] = uniforms.colorBaseT
  u32[23] = uniforms.colorBaseN
  u32[24] = uniforms.colorInsertionIndicator
  u32[25] = uniforms.colorSoftclipIndicator
  u32[26] = uniforms.colorHardclipIndicator
}

export const INSTANCE_STRIDE_BYTES = 20
export const INSTANCE_STRIDE_WORDS = 5

// Word indices into a Float32Array view over the instance buffer.
export const INSTANCE_OFFSET_F32 = {
  yOffset: 1,
  segHeight: 2,
  relDepth: 4,
} as const

// Word indices into a Uint32Array view over the instance buffer.
export const INSTANCE_OFFSET_U32 = {
  position: 0,
  packedColor: 3,
} as const

export const VERTEX_ATTRIBUTES: readonly VertexAttributeLayout[] = [
  { name: 'a_position', components: 1, type: 'uint', offsetBytes: 0, integer: true },
  { name: 'a_yOffset', components: 1, type: 'float', offsetBytes: 4, integer: false },
  { name: 'a_segHeight', components: 1, type: 'float', offsetBytes: 8, integer: false },
  { name: 'a_packedColor', components: 1, type: 'uint', offsetBytes: 12, integer: true },
  { name: 'a_relDepth', components: 1, type: 'float', offsetBytes: 16, integer: false },
]

export interface InstanceArrays {
  position: ArrayLike<number>
  yOffset: ArrayLike<number>
  segHeight: ArrayLike<number>
  packedColor: ArrayLike<number>
  relDepth: ArrayLike<number>
}

export function packInstances(
  arrays: InstanceArrays,
  numInstances: number,
  buf: ArrayBuffer = new ArrayBuffer(numInstances * INSTANCE_STRIDE_BYTES),
) {
  const f32 = new Float32Array(buf)
  const u32 = new Uint32Array(buf)
  const { position, yOffset, segHeight, packedColor, relDepth } = arrays
  for (let i = 0; i < numInstances; i++) {
    const o = i * INSTANCE_STRIDE_WORDS
    u32[o + 0] = position[i]!
    f32[o + 1] = yOffset[i]!
    f32[o + 2] = segHeight[i]!
    u32[o + 3] = packedColor[i]!
    f32[o + 4] = relDepth[i]!
  }
  return buf
}

// Instance `i`'s `position`.
export function getInstancePosition(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS]!
}

export function setInstancePosition(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS] = v
}

// Instance `i`'s `yOffset`.
export function getInstanceYOffset(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 1]!
}

export function setInstanceYOffset(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 1] = v
}

// Instance `i`'s `segHeight`.
export function getInstanceSegHeight(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 2]!
}

export function setInstanceSegHeight(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 2] = v
}

// Instance `i`'s `packedColor`.
export function getInstancePackedColor(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 3]!
}

export function setInstancePackedColor(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 3] = v
}

// Instance `i`'s `relDepth`.
export function getInstanceRelDepth(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 4]!
}

export function setInstanceRelDepth(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 4] = v
}
