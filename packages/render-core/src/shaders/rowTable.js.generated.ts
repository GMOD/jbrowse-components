// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from rowTable.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of rowTable.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function _max(a: number, b: number) {
  return b > a || Number.isNaN(a) ? b : a
}

function _min(a: number, b: number) {
  return b < a || Number.isNaN(a) ? b : a
}

export function rowTableWidth(keys: number): number {
  return _max(_min(keys, 2048), 1)
}

export function rowTablePlaneHeight(keys: number): number {
  let w = rowTableWidth(keys)
  let _t0 = Math.trunc(((((keys + w) >>> 0) - 1) >>> 0) / w)
  return _max(_t0, 1)
}

export function rowTableTexelX(key: number, keys: number): number {
  let _t0 = (key % rowTableWidth(keys))
  return _t0
}

export function rowTableTexelY(key: number, keys: number): number {
  let _t0 = Math.trunc(key / rowTableWidth(keys))
  return _t0
}
