// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from antialias.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of antialias.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function _clamp(x: number, lo: number, hi: number) {
  return _min(_max(x, lo), hi)
}

function _max(a: number, b: number) {
  return b > a || Number.isNaN(a) ? b : a
}

function _min(a: number, b: number) {
  return b < a || Number.isNaN(a) ? b : a
}

export function aaPx(dpr: number): number {
  return (1.0 / _max(dpr, 1.0))
}

export function aaHalfPx(dpr: number): number {
  return (0.5 * aaPx(dpr))
}

export function aaRamp(signedInk: number, widthPx: number): number {
  return _clamp(((signedInk / widthPx) + 0.5), 0.0, 1.0)
}

export function edgeCoverage(signedInkCssPx: number, dpr: number): number {
  return aaRamp(signedInkCssPx, aaPx(dpr))
}
