// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from hpmath.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of hpmath.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

export function snapBoxHeightPx(heightPx: number): number {
  let hPx = Math.floor((heightPx + 0.5))
  let _S1: boolean
  if (((hPx - (2.0 * Math.floor((hPx * 0.5)))) == 0.0)) {
    _S1 = (hPx >= 2.0)
  } else {
    _S1 = false
  }
  if (_S1) {
    _S1 = (hPx <= 4.0)
  } else {
    _S1 = false
  }
  let _S2: number
  if (_S1) {
    _S2 = (hPx + 1.0)
  } else {
    _S2 = hPx
  }
  return _S2
}

export function snapBoxCenterYPx(centerY: number, heightPx: number, scrollY: number): number {
  return ((Math.floor((((centerY - (heightPx * 0.5)) - scrollY) + 0.5)) + Math.floor((snapBoxHeightPx(heightPx) * 0.5))) + 0.5)
}

export function extendToMinWidthPx(x1: number, x2: number, minWidth: number): number {
  let dx = (x2 - x1)
  let _S3: number
  if ((Math.abs(dx) < minWidth)) {
    if ((dx < 0.0)) {
      _S3 = -minWidth
    } else {
      _S3 = minWidth
    }
    _S3 = (x1 + _S3)
  } else {
    _S3 = x2
  }
  return _S3
}
