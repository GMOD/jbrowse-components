// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from lineMark.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

import type { VertexAttributeLayout, ShaderBinding, TextureBinding } from '@jbrowse/render-core/hal'

export const BINDINGS: readonly ShaderBinding[] = [
  { index: 1, kind: 'uniform', name: 'u' },
  { index: 2, kind: 'texture', name: 'colorRamp' },
  { index: 3, kind: 'sampler', name: 'colorRamp' },
]

export const VERTS_PER_INSTANCE = 6

export const UNIFORMS_SIZE_BYTES = 80

// Word indices into a Float32Array view over the uniform buffer.
export const UNIFORM_OFFSET_F32 = {
  bpRangeX: 0,
  canvasHeight: 3,
  domainMin: 4,
  domainMax: 5,
  rampMin: 8,
  rampMax: 9,
  rowHeight: 10,
  origin: 11,
  lineWidth: 12,
  zero: 14,
  viewportWidth: 15,
  devicePixelRatio: 16,
} as const

// Word indices into a Int32Array view over the uniform buffer.
export const UNIFORM_OFFSET_I32 = {
  valueScaleType: 6,
  rampMode: 7,
  variant: 13,
} as const


export interface Uniforms {
  bpRangeX: [number, number, number]
  canvasHeight: number
  domainMin: number
  domainMax: number
  valueScaleType: number
  rampMode: number
  rampMin: number
  rampMax: number
  rowHeight: number
  origin: number
  lineWidth: number
  variant: number
  zero: number
  viewportWidth: number
  devicePixelRatio: number
}

export function writeUniforms(buf: ArrayBuffer, uniforms: Uniforms) {
  const f32 = new Float32Array(buf)
  const i32 = new Int32Array(buf)
  f32[0] = uniforms.bpRangeX[0]
  f32[1] = uniforms.bpRangeX[1]
  f32[2] = uniforms.bpRangeX[2]
  f32[3] = uniforms.canvasHeight
  f32[4] = uniforms.domainMin
  f32[5] = uniforms.domainMax
  i32[6] = uniforms.valueScaleType
  i32[7] = uniforms.rampMode
  f32[8] = uniforms.rampMin
  f32[9] = uniforms.rampMax
  f32[10] = uniforms.rowHeight
  f32[11] = uniforms.origin
  f32[12] = uniforms.lineWidth
  i32[13] = uniforms.variant
  f32[14] = uniforms.zero
  f32[15] = uniforms.viewportWidth
  f32[16] = uniforms.devicePixelRatio
}

export const INSTANCE_STRIDE_BYTES = 40
export const INSTANCE_STRIDE_WORDS = 10

// Word indices into a Float32Array view over the instance buffer.
export const INSTANCE_OFFSET_F32 = {
  y: 2,
  prevY: 5,
  nextY: 6,
  prevYLine: 9,
} as const

// Word indices into a Uint32Array view over the instance buffer.
export const INSTANCE_OFFSET_U32 = {
  x: 0,
  x2: 1,
  color: 3,
  row: 4,
  prevX: 7,
  prevX2: 8,
} as const

export const VERTEX_ATTRIBUTES: readonly VertexAttributeLayout[] = [
  { name: 'a_x', components: 1, type: 'uint', offsetBytes: 0, integer: true },
  { name: 'a_x2', components: 1, type: 'uint', offsetBytes: 4, integer: true },
  { name: 'a_y', components: 1, type: 'float', offsetBytes: 8, integer: false },
  { name: 'a_color', components: 1, type: 'uint', offsetBytes: 12, integer: true },
  { name: 'a_row', components: 1, type: 'uint', offsetBytes: 16, integer: true },
  { name: 'a_prevY', components: 1, type: 'float', offsetBytes: 20, integer: false },
  { name: 'a_nextY', components: 1, type: 'float', offsetBytes: 24, integer: false },
  { name: 'a_prevX', components: 1, type: 'uint', offsetBytes: 28, integer: true },
  { name: 'a_prevX2', components: 1, type: 'uint', offsetBytes: 32, integer: true },
  { name: 'a_prevYLine', components: 1, type: 'float', offsetBytes: 36, integer: false },
]

export interface InstanceArrays {
  x: ArrayLike<number>
  x2: ArrayLike<number>
  y: ArrayLike<number>
  color: ArrayLike<number>
  row: ArrayLike<number>
  prevY: ArrayLike<number>
  nextY: ArrayLike<number>
  prevX: ArrayLike<number>
  prevX2: ArrayLike<number>
  prevYLine: ArrayLike<number>
}

export function packInstances(
  arrays: InstanceArrays,
  numInstances: number,
  buf: ArrayBuffer = new ArrayBuffer(numInstances * INSTANCE_STRIDE_BYTES),
) {
  const f32 = new Float32Array(buf)
  const u32 = new Uint32Array(buf)
  const { x, x2, y, color, row, prevY, nextY, prevX, prevX2, prevYLine } = arrays
  for (let i = 0; i < numInstances; i++) {
    const o = i * INSTANCE_STRIDE_WORDS
    u32[o + 0] = x[i]!
    u32[o + 1] = x2[i]!
    f32[o + 2] = y[i]!
    u32[o + 3] = color[i]!
    u32[o + 4] = row[i]!
    f32[o + 5] = prevY[i]!
    f32[o + 6] = nextY[i]!
    u32[o + 7] = prevX[i]!
    u32[o + 8] = prevX2[i]!
    f32[o + 9] = prevYLine[i]!
  }
  return buf
}

// Instance `i`'s `x`.
export function getInstanceX(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS]!
}

export function setInstanceX(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS] = v
}

// Instance `i`'s `x2`.
export function getInstanceX2(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 1]!
}

export function setInstanceX2(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 1] = v
}

// Instance `i`'s `y`.
export function getInstanceY(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 2]!
}

export function setInstanceY(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 2] = v
}

// Instance `i`'s `color`.
export function getInstanceColor(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 3]!
}

export function setInstanceColor(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 3] = v
}

// Instance `i`'s `row`.
export function getInstanceRow(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 4]!
}

export function setInstanceRow(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 4] = v
}

// Instance `i`'s `prevY`.
export function getInstancePrevY(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 5]!
}

export function setInstancePrevY(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 5] = v
}

// Instance `i`'s `nextY`.
export function getInstanceNextY(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 6]!
}

export function setInstanceNextY(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 6] = v
}

// Instance `i`'s `prevX`.
export function getInstancePrevX(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 7]!
}

export function setInstancePrevX(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 7] = v
}

// Instance `i`'s `prevX2`.
export function getInstancePrevX2(u32: Uint32Array, i: number) {
  return u32[i * INSTANCE_STRIDE_WORDS + 8]!
}

export function setInstancePrevX2(u32: Uint32Array, i: number, v: number) {
  u32[i * INSTANCE_STRIDE_WORDS + 8] = v
}

// Instance `i`'s `prevYLine`.
export function getInstancePrevYLine(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 9]!
}

export function setInstancePrevYLine(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 9] = v
}

// Combined `Sampler2D` bindings. Texture unit indices start at 0.
export const TEXTURES: readonly [TextureBinding, ...TextureBinding[]] = [
  { textureBinding: 2, samplerBinding: 3, glTextureUnit: 0, glUniformName: 'u_colorRamp', filter: 'linear' },
]
