{
  "name": "jbrowse-newsletter",
  "private": true,
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "deploy:update": "sam deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3",
    "@aws-sdk/client-ses": "^3",
    "@aws-sdk/lib-dynamodb": "^3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8",
    "typescript": "^5"
  }
}
