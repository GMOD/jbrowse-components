// Padding added to both sides of every launched panel, and of a linear view
// opened on a mate's matching region. Its own module rather than a corner of
// the option fields: a menu builder reads it, and importing the fields for one
// number pulls the MUI Accordion into a display's module graph.
export const DEFAULT_WINDOW_SIZE = 1000
