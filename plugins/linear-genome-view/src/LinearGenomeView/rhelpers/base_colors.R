# JBrowse's per-base mismatch colors (green A / blue C / orange G / red T /
# brown N), used with scale_fill_identity so no legend and no second fill scale.
base_colors <- c(A = "#4caf50", C = "#2196f3", G = "#ff9800", T = "#f44336", N = "#795548")
