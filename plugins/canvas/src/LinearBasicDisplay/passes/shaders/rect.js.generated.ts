// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from rect.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of rect.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function extendToMinWidthPx(x1: number, x2: number, minWidth: number): number {
  let dx = (x2 - x1)
  let _t0: number
  if ((Math.abs(dx) < minWidth)) {
    if ((dx < 0.0)) {
      _t0 = -minWidth
    } else {
      _t0 = minWidth
    }
    _t0 = (x1 + _t0)
  } else {
    _t0 = x2
  }
  return _t0
}

export function rectSpanPx(x1Px: number, x2Px: number, isPoint: boolean): [number, number] {
  if (isPoint) {
    return [Math.floor(((x1Px - 1.0) + 0.5)), Math.floor(((x1Px + 1.0) + 0.5))]
  }
  let left = Math.floor((x1Px + 0.5))
  return [left, extendToMinWidthPx(left, Math.floor((x2Px + 0.5)), 2.0)]
}

export function rectDrawsOutline(widthPx: number, heightPx: number): boolean {
  let _t0: boolean
  if ((widthPx > 2.0)) {
    _t0 = (heightPx > 2.0)
  } else {
    _t0 = false
  }
  return _t0
}
