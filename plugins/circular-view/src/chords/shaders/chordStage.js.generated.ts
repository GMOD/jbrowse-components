// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from chordStage.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of chordStage.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function _max(a: number, b: number) {
  return b > a || Number.isNaN(a) ? b : a
}

export function chordTurn(from: number, to: number): number {
  let t = (to - from)
  return (t - (6.28318548202514648 * Math.floor(((t + 3.14159274101257324) / 6.28318548202514648))))
}

export function ribbonEndPad(a: number, b: number, radius: number, minWidthPx: number): number {
  return (_max(0.0, ((minWidthPx / _max(radius, 1.0)) - Math.abs((b - a)))) * 0.5)
}

export function chordIsSpeck(a: number, b: number, radius: number): boolean {
  return ((Math.abs(chordTurn(a, b)) * radius) < 1.0)
}
