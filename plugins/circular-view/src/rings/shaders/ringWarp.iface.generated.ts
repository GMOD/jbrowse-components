// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from ringWarp.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

import type { VertexAttributeLayout, ShaderBinding, BlendState, TextureBinding } from '@jbrowse/render-core/hal'

export const BINDINGS: readonly ShaderBinding[] = [
  { index: 1, kind: 'uniform', name: 'u' },
  { index: 2, kind: 'texture', name: 'strip' },
  { index: 3, kind: 'sampler', name: 'strip' },
]

export const VERTS_PER_INSTANCE = 384

export const BLEND_STATE: BlendState = { srcFactor: 'one', dstFactor: 'one-minus-src-alpha' }

export const COVERAGE = 'analytic' as const

export const UNIFORMS_SIZE_BYTES = 32

// Word indices into a Float32Array view over the uniform buffer.
export const UNIFORM_OFFSET_F32 = {
  centerX: 0,
  centerY: 1,
  canvasWidth: 2,
  canvasHeight: 3,
  offsetRadians: 4,
  devicePixelRatio: 5,
} as const


export interface Uniforms {
  centerX: number
  centerY: number
  canvasWidth: number
  canvasHeight: number
  offsetRadians: number
  devicePixelRatio: number
}

export function writeUniforms(buf: ArrayBuffer, uniforms: Uniforms) {
  const f32 = new Float32Array(buf)
  f32[0] = uniforms.centerX
  f32[1] = uniforms.centerY
  f32[2] = uniforms.canvasWidth
  f32[3] = uniforms.canvasHeight
  f32[4] = uniforms.offsetRadians
  f32[5] = uniforms.devicePixelRatio
}

export const INSTANCE_STRIDE_BYTES = 8
export const INSTANCE_STRIDE_WORDS = 2

// Word indices into a Float32Array view over the instance buffer.
export const INSTANCE_OFFSET_F32 = {
  innerPx: 0,
  outerPx: 1,
} as const

export const VERTEX_ATTRIBUTES: readonly VertexAttributeLayout[] = [
  { name: 'a_innerPx', components: 1, type: 'float', offsetBytes: 0, integer: false },
  { name: 'a_outerPx', components: 1, type: 'float', offsetBytes: 4, integer: false },
]

export interface InstanceArrays {
  innerPx: ArrayLike<number>
  outerPx: ArrayLike<number>
}

export function packInstances(
  arrays: InstanceArrays,
  numInstances: number,
  buf: ArrayBuffer = new ArrayBuffer(numInstances * INSTANCE_STRIDE_BYTES),
) {
  const f32 = new Float32Array(buf)
  const { innerPx, outerPx } = arrays
  for (let i = 0; i < numInstances; i++) {
    const o = i * INSTANCE_STRIDE_WORDS
    f32[o + 0] = innerPx[i]!
    f32[o + 1] = outerPx[i]!
  }
  return buf
}

// Instance `i`'s `innerPx`.
export function getInstanceInnerPx(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS]!
}

export function setInstanceInnerPx(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS] = v
}

// Instance `i`'s `outerPx`.
export function getInstanceOuterPx(f32: Float32Array, i: number) {
  return f32[i * INSTANCE_STRIDE_WORDS + 1]!
}

export function setInstanceOuterPx(f32: Float32Array, i: number, v: number) {
  f32[i * INSTANCE_STRIDE_WORDS + 1] = v
}

// Combined `Sampler2D` bindings. Texture unit indices start at 0.
export const TEXTURES: readonly [TextureBinding, ...TextureBinding[]] = [
  { textureBinding: 2, samplerBinding: 3, glTextureUnit: 0, glUniformName: 'u_strip', filter: 'linear' },
]
