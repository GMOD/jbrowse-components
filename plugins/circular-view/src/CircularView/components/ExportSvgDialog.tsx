// re-exported in a local module so the view's lazy() import stays local, the
// same way the LGV-family views re-export their shared dialog
export { ExportSvgDialog as default } from '@jbrowse/core/ui'
