import React from 'react'

// Unused stub — rendering is handled by the GPU display component.
// FeatureRendererType requires a ReactComponent so this exists as a placeholder.
export default function LinearManhattanRendering() {
  return <></>
}
