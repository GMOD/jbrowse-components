/** The field the LD join writes: a SNP's r² to the index, 1 on the index. */
export const LD_FIELD = 'ld'

/** The field naming a joined SNP's part: `index`, or `partner` of it. */
export const LD_ROLE_FIELD = 'ld_role'
