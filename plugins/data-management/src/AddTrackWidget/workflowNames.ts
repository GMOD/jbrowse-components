// Names used both to register add-track workflows and to switch between them.
// Kept in one place so a rename can't silently break the cross-workflow links
// (e.g. the "Add multiple tracks at once" link in the default workflow).
export const DEFAULT_WORKFLOW = 'Default add track workflow'
export const PASTE_JSON_WORKFLOW = 'Add track JSON'
export const BULK_WORKFLOW = 'Add multiple tracks'
