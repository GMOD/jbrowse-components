// In its own leaf module so the components chunk can read it without pulling
// the (lazily loaded) state model module into its chunk.

/** height of the "show only regions with data" bar above the circular view */
export const circularViewOptionsBarHeight = 52
