// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from overlap.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of overlap.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function _smoothstep(e0: number, e1: number, x: number) {
  const r = (x - e0) / (e1 - e0)
  const above = r > 0 ? r : 0
  const t = above < 1 ? above : 1
  return t * t * (3 - 2 * t)
}

export function overlapAlpha(pxWidth: number): number {
  return (0.40000000596046448 * _smoothstep(1.5, 12.0, pxWidth))
}
