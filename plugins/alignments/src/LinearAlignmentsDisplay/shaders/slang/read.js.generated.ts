// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from read.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

// Scalar twins of read.slang, transliterated from slangc's WGSL so
// the Canvas2D and SVG paths run the shader's own math. See adr-051.

function _clamp(x: number, lo: number, hi: number) {
  return Math.min(Math.max(x, lo), hi)
}

export function showChevron(chainMode: boolean, pxPerBp: number, featHeight: number, colorScheme: number, flags: number, interchrom: number, insertSize: number, featWPx: number): boolean {
  let baseShow: boolean
  if (chainMode) {
    baseShow = true
  } else {
    baseShow = (pxPerBp > 0.10000000149011612)
  }
  if (baseShow) {
    baseShow = (featHeight >= 3.0)
  } else {
    baseShow = false
  }
  let dirMoot: boolean
  if ((colorScheme == ((0) >>> 0))) {
    dirMoot = true
  } else {
    dirMoot = (((flags & ((8) >>> 0)) >>> 0) != ((0) >>> 0))
  }
  if (dirMoot) {
    dirMoot = true
  } else {
    dirMoot = (interchrom != ((0) >>> 0))
  }
  let pairTooTight: boolean
  if ((((flags & ((1) >>> 0)) >>> 0) != ((0) >>> 0))) {
    pairTooTight = ((Math.abs(insertSize) * pxPerBp) < 10.0)
  } else {
    pairTooTight = false
  }
  if (baseShow) {
    baseShow = !pairTooTight
  } else {
    baseShow = false
  }
  if (baseShow) {
    if (!dirMoot) {
      baseShow = true
    } else {
      baseShow = (featWPx > 30.0)
    }
  } else {
    baseShow = false
  }
  return baseShow
}

export function insertGradientT(is: number, lower: number, upper: number, isLong: boolean): number {
  let span = ((upper - lower) * 0.5)
  if ((span <= 0.0)) {
    return 0.0
  }
  let d: number
  if (isLong) {
    d = (is - upper)
  } else {
    d = (lower - is)
  }
  return _clamp((d / span), 0.0, 1.0)
}
