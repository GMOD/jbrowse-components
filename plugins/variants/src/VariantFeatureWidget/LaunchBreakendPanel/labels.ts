// The wording of the split-view link, shared by the breakend and contiguous-SV
// panels and imported by the website's link_to_split_view spec, which anchors a
// callout on it. Its own module so that spec (run under node, no React) can read
// it without pulling in the components.
export const SPLIT_VIEW_LINK_LABEL = 'Open breakpoints in split view'
