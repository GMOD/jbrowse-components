// AUTO-GENERATED by packages/shader-tools/src/shader-codegen from ldGenomic.slang.
// Do not edit. Run `pnpm gen:shaders` to regenerate.

import type { GlAttributeLayout, TextureBinding } from '@jbrowse/render-core/hal'

export const VERTS_PER_INSTANCE = 6

export const UNIFORMS_SIZE_BYTES = 32

// Indices into a Float32Array / Uint32Array view over the uniform buffer.
export const UNIFORM_OFFSET_F32 = {
  canvasSize: 0,
  yScalar: 2,
  viewScale: 3,
  viewOffsetX: 4,
  signedLd: 5,
  uniformW: 6,
} as const


export interface Uniforms {
  canvasSize: [number, number]
  yScalar: number
  viewScale: number
  viewOffsetX: number
  signedLd: number
  uniformW: number
}

export function writeUniforms(buf: ArrayBuffer, uniforms: Uniforms) {
  const f32 = new Float32Array(buf)
  const u32 = new Uint32Array(buf)
  f32[0] = uniforms.canvasSize[0]
  f32[1] = uniforms.canvasSize[1]
  f32[2] = uniforms.yScalar
  f32[3] = uniforms.viewScale
  f32[4] = uniforms.viewOffsetX
  u32[5] = uniforms.signedLd
  f32[6] = uniforms.uniformW
}

export const INSTANCE_STRIDE_BYTES = 20
export const INSTANCE_STRIDE_F32 = 5

export const FIELD_OFFSET_F32 = {
  position: 0,
  cellSize: 2,
  ldValue: 4,
} as const

export const GL_ATTRIBUTES: readonly GlAttributeLayout[] = [
  { name: 'a_position', components: 2, type: 'float', offsetBytes: 0, integer: false },
  { name: 'a_cellSize', components: 2, type: 'float', offsetBytes: 8, integer: false },
  { name: 'a_ldValue', components: 1, type: 'float', offsetBytes: 16, integer: false },
]

export interface InstanceArrays {
  position: ArrayLike<number>
  cellSize: ArrayLike<number>
  ldValue: ArrayLike<number>
}

export function packInstances(
  arrays: InstanceArrays,
  numInstances: number,
  buf: ArrayBuffer = new ArrayBuffer(numInstances * INSTANCE_STRIDE_BYTES),
) {
  const f32 = new Float32Array(buf)
  const { position, cellSize, ldValue } = arrays
  for (let i = 0; i < numInstances; i++) {
    const o = i * INSTANCE_STRIDE_F32
    f32[o + 0] = position[i * 2 + 0]!
    f32[o + 1] = position[i * 2 + 1]!
    f32[o + 2] = cellSize[i * 2 + 0]!
    f32[o + 3] = cellSize[i * 2 + 1]!
    f32[o + 4] = ldValue[i]!
  }
  return buf
}

// Combined `Sampler2D` bindings. Texture unit indices start at 0.
export const TEXTURES: readonly [TextureBinding, ...TextureBinding[]] = [
  { textureBinding: 2, samplerBinding: 3, glTextureUnit: 0, glUniformName: 'u_colorRamp', filter: 'linear' },
]
