{
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "s=.claude/hooks/setup-worktree.sh; [ -x \"$s\" ] || s=\"$CLAUDE_PROJECT_DIR/$s\"; if [ -x \"$s\" ]; then exec \"$s\"; else cat >/dev/null; echo '{\"systemMessage\":\"worktree setup hook not found - run pnpm install in the new worktree yourself\"}'; fi",
            "timeout": 600,
            "statusMessage": "Installing dependencies in the new worktree..."
          }
        ]
      }
    ]
  }
}
