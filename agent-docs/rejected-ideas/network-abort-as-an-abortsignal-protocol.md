---
name: network-abort-as-an-abortsignal-protocol
description: Network abort as an `AbortSignal` protocol
area: performance-and-measurement
---

# Network abort as an `AbortSignal` protocol

cancellation already reaches
the socket via one stop token, and the two unwirable readers stay unwirable.
[NETWORK_ABORT.md](../reference/NETWORK_ABORT.md).
