---
name: matchesslotshape-delegating-to-mst-model-is
description: `matchesSlotShape` delegating to MST `model.is()`
area: tooling-tests-and-docs
---

# `matchesSlotShape` delegating to MST `model.is()`

too permissive; admits
`NaN` and frozen values.
