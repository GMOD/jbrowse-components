---
name: canvas2d-glyph-atlas-for-alignment-labels
description: Canvas2D glyph atlas for alignment labels
area: rendering-and-displays
---

# Canvas2D glyph atlas for alignment labels

3x worse than `fillText`,
which is ~85% of `drawAlignmentLabels` at ~1µs/glyph. At the floor.
