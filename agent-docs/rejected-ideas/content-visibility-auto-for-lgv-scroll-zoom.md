---
name: content-visibility-auto-for-lgv-scroll-zoom
description: `content-visibility: auto` for LGV scroll-zoom
area: rendering-and-displays
---

# `content-visibility: auto` for LGV scroll-zoom

measured regression. CSS
is ~33% of frame on GPU backends vs ~3% on canvas2d, so CSS levers look more
attractive here than they are.
