---
name: column-encoder-verdict
description: One of four kill criteria fired — a column encode matches wiggle's hand packer in time and beats the SimpleFeature path 5-10x, but EncodedChannels retains 20 bytes a feature against wiggle's 12, four of them an identity featureIndex and four a per-instance colour lane that wiggle carries per source
---

# The column encoder, measured against wiggle's hand path

Measured 2026-09-19 on branch `worktree-agent-a57482aeda0007067`, from the spike
`packages/core/benches/columnTable.ts` and the bench
`packages/core/benches/columnEncode.bench.ts`. Nothing here landed: the spike is
bench-only and no production code moved.

## The question

Every measured refusal to move a format-typed display onto the mark grammar
blames the `Feature[]`, not the grammar's rule.
[ADR-114](../architecture-decision-records/adr-114-canvas-keeps-its-hand-written-packer.md)
priced canvas's packer against the encoder at 3.11x,
[ADR-118](../architecture-decision-records/adr-118-the-packers-share-a-rule-not-a-step.md)
priced the pileup's layout against the `stack` step at 4.23x and said in as many
words that "the packing is not what costs — the `Feature[]` on both sides of it
is", and
[ADR-127](../architecture-decision-records/adr-127-line-stays-wiggles.md)
refused a `line` shape over 16 retained bytes a feature rather than over speed.

Wiggle's main path never builds a `Feature`. `RawFeatureArrays {starts, ends,
scores}` goes straight into interleaved instance records, `EncodedChannels` is
already a struct of arrays, and `bar`'s 20-byte instance record is wiggle's fill
record word for word. So the substitution worth pricing is the encoder reading
typed columns: an `encodeColumns` emitting the same `EncodedChannels`, the
transform steps as index-array kernels, and one reusable row cursor implementing
`Feature` so a `jexl:` channel evaluates without a `SimpleFeature`.

## The encode

<!-- BEGIN GENERATED MEASUREMENT column-encode -->

_Generated by `pnpm autogen` — edit the source, not this block._

| scenario     |       rows |  wiggle | control | features | columns | control vs wiggle | features vs wiggle | columns vs wiggle | wiggle B/feature | encoded B/feature | bytes vs wiggle |
| ------------ | ---------: | ------: | ------: | -------: | ------: | ----------------: | -----------------: | ----------------: | ---------------: | ----------------: | --------------: |
| volvox raw   |    500,000 |   9.5ms |   9.7ms |   82.6ms |   9.4ms |             1.02x |              8.69x |         **0.99x** |               12 |                20 |       **1.67x** |
| synthetic    |    500,000 |  10.1ms |   9.9ms |   80.5ms |   8.3ms |             0.98x |              7.97x |         **0.82x** |               12 |                20 |       **1.67x** |
| synthetic    |  1,000,000 |  24.8ms |  23.7ms |  247.7ms |  16.7ms |             0.96x |              9.99x |             0.67x |               12 |                20 |           1.67x |
| multi-wiggle | 14,000,000 | 325.6ms | 323.7ms | 1658.7ms | 190.7ms |             0.99x |              5.09x |         **0.59x** |               12 |                20 |       **1.67x** |

<!-- END GENERATED MEASUREMENT column-encode -->

The two encodes agree lane for lane, both agree with wiggle's positions and
scores row for row, and wiggle's fill record and `bar`'s instance record come
out byte-identical — the bench refuses to time anything until they do.

## The `jexl:` escape

<!-- BEGIN GENERATED MEASUREMENT column-encode-jexl -->

_Generated by `pnpm autogen` — edit the source, not this block._

| scenario     |       rows | features-jexl | columns-cursor-jexl | cursor vs features |
| ------------ | ---------: | ------------: | ------------------: | -----------------: |
| volvox raw   |    500,000 |       192.1ms |              55.6ms |          **0.29x** |
| synthetic    |    500,000 |       202.2ms |              55.1ms |          **0.27x** |
| synthetic    |  1,000,000 |       444.4ms |             113.8ms |          **0.26x** |
| multi-wiggle | 14,000,000 |      3895.3ms |            1547.6ms |          **0.40x** |

<!-- END GENERATED MEASUREMENT column-encode-jexl -->

One `RowCursor` whose index moves builds the jexl proxy and the context object
once per region instead of once per row, which is the whole of the difference.

## The kernels

<!-- BEGIN GENERATED MEASUREMENT column-kernels -->

_Generated by `pnpm autogen` — edit the source, not this block._

| scenario   | arm               |   rows in |    wall | per row (ns) | reference | vs reference |
| ---------- | ----------------- | --------: | ------: | -----------: | --------: | -----------: |
| stack      | layout            |   200,000 |  13.3ms |           67 |    13.3ms |        1.00x |
| stack      | control           |   200,000 |  13.3ms |           67 |    13.3ms |        1.00x |
| stack      | stack             |   200,000 |  57.9ms |          290 |    13.3ms |    **4.35x** |
| stack      | step              |   200,000 |  29.0ms |          145 |    13.3ms |        2.18x |
| stack      | features          |   200,000 |  15.3ms |           77 |    13.3ms |        1.15x |
| stack      | stack-columns     |   200,000 |  16.2ms |           81 |    13.3ms |    **1.22x** |
| stack      | stack-columns-cmp |   200,000 |  30.7ms |          154 |    13.3ms |        2.31x |
| transforms | none              | 1,000,000 |  90.8ms |           91 |    90.8ms |        1.00x |
| transforms | control           | 1,000,000 |  90.8ms |           91 |    90.8ms |        1.00x |
| transforms | bin-count         | 1,000,000 | 338.6ms |          339 |    90.8ms |    **3.73x** |
| transforms | bin-count-columns | 1,000,000 |  18.2ms |           18 |    90.8ms |    **0.20x** |
| transforms | coverage          | 1,000,000 | 594.7ms |          595 |    90.8ms |    **6.55x** |
| transforms | coverage-columns  | 1,000,000 | 156.6ms |          157 |    90.8ms |    **1.72x** |

<!-- END GENERATED MEASUREMENT column-kernels -->

The stack and transform scenarios rerun the neighbouring benches' own arms in
the same sitting rather than quoting them, and reproduce their recorded rows.

## Which criterion fired

Four were stated in the bench header before the run. One fired.

- **`columns` above 1.10x `wiggle` in time** — did not fire.
  0.59-0.99x<!--m:column-encode.columnsVsWiggle.range--> across the four
  fixtures, against a control holding
  0.96-1.02x<!--m:column-encode.controlVsWiggle.range-->. The column encode is
  at worst parity with the packer that ships and is faster wherever the region
  is big.
- **Retained bytes above wiggle's 12 plus 4** — **fired**, on every fixture.
  `EncodedChannels` holds
  20<!--m:column-encode.volvox-raw.encodedBytes--> bytes a feature where wiggle
  holds 12<!--m:column-encode.volvox-raw.wiggleBytes-->, and the figure does not
  move with scale because it is the lane set: `x`, `x2`, `y`, `featureIndex` and
  `color` at four bytes each, against interleaved positions and scores.
- **`stack-columns` above 1.5x `layout`** — did not fire.
  1.22x<!--m:column-kernels.stack-stack-columns.vsBase--> against the
  hand-written layout, where the same packing through the `stack` step is
  4.35x<!--m:column-kernels.stack-stack.vsBase-->.
- **`columns-cursor-jexl` above 1.2x `features-jexl`** — did not fire.
  0.26-0.40x<!--m:column-encode-jexl.cursorVsFeatures.range-->.

## What the three refusals' premises look like now

**The `features` arm is a `SimpleFeature` per row, and no display runs that
path.** A BAM, CRAM or VCF adapter hands the encoder its own feature objects
and the encoder reads `feature.get(field)` off them; the BigWig adapter hands
it a two-field `BigWigFeature` cursor over the array view it already holds
(`plugins/wiggle/src/BigWigAdapter/BigWigAdapter.ts`), which is the row cursor
the spike reinvents. So the
5.09-9.99x<!--m:column-encode.featuresVsWiggle.range--> below prices building
a `SimpleFeature` per row, and the shipping mark display over a BigWig is
unmeasured; a `BigWigFeature` arm comes before this figure is quoted anywhere
else.

**ADR-114's number was the `Feature[]` and the lane mismatch, and its decision
was neither.** The `SimpleFeature` encode runs
5.09-9.99x<!--m:column-encode.featuresVsWiggle.range--> the hand packer here,
which is ADR-114's 3.11x met again on a path with five lanes rather than
twenty-nine; the column encode runs at parity. What that does not touch is the
half of ADR-114 that has no number in it — seventeen lanes with no channel, a
window rule that is closed at both ends for a degenerate span, and four side
outputs the walk emits as it goes. Canvas's packer stays hand-written for those,
and a column table changes none of them.

**ADR-118's own explanation is confirmed, and its audit still stands.** The step
costs 4.35x<!--m:column-kernels.stack-stack.vsBase--> the layout with the
`SimpleFeature` construction inside the timing, and
2.18x<!--m:column-kernels.stack-step.vsBase--> over a prebuilt list, which is
the row a BAM's own feature objects put a declared pileup on; the column
kernel costs
1.22x<!--m:column-kernels.stack-stack-columns.vsBase-->, so the representation
was indeed the whole of the gap: the packing rule is the same rule either way.
The `bin` and `coverage` kernels say it louder —
0.20x<!--m:column-kernels.transforms-bin-count-columns.vsBase--> and
1.72x<!--m:column-kernels.transforms-coverage-columns.vsBase--> of the bare
encode against
3.73x<!--m:column-kernels.transforms-bin-count.vsBase--> and
6.55x<!--m:column-kernels.transforms-coverage.vsBase--> for the Feature-typed
steps. But ADR-118 refused the port on an audit, not on the ratio: caps,
comparators, cross-region grouping, label widths and a frame-seeded layout are
display state a worker step is never told, and a faster kernel does not tell it.
The ADR's conclusion is unchanged; only its arithmetic moves.

**ADR-127's premise is the one that survives intact, and it is the criterion
that fired.** ADR-127 read the cost as the lens — "`x`, `x2`, a colour lane and
a row lane are 16 bytes a feature held per region beside the 40-byte record" —
and predicted the shape would come free "for the day wiggle's payload is
per-instance lanes". This bench is that day, and the day costs eight bytes a
feature: four for `featureIndex`, which is the identity permutation whenever no
feature is skipped, and four for a per-instance colour lane, which wiggle
carries once per source. Speed was never ADR-127's kill and is not this one's
either.

## What WP6 would have to do

A real column encoder, wiggle's xyplot on the mark list and then a `rect` shape
with `y2` are on, on four conditions the numbers name:

- **`EncodedChannels` has to be able to decline `featureIndex`.** A column
  encode over a table with no unplaceable row answers the identity permutation,
  and every consumer that reads it (Manhattan's r², wiggle's summary band) can
  index directly instead. Four of the eight bytes.
- **A constant colour has to ride as a scalar, not a lane.** The other four, and
  the same conclusion
  [wiggle-instance-records-carry-per-row-constants](wiggle-instance-records-carry-per-row-constants.md)
  reached from the GPU side in its §3: a per-row colour table shrinks every
  record and turns a recolour into one small upload. Met from the encoder side
  it also shrinks the payload the worker ships and `rpcDataMap` retains.
- **A multi-wiggle encodes one layer per source, with the row as a per-layer
  constant.** The `multi-wiggle` row above is measured that way, which is the
  framing most favourable to the mark path; a faceted form writing `FACET_ROW`
  per feature adds a fifth four-byte lane and puts the encoder back above
  wiggle.
- **The `stack` kernel sorts without a comparator.** The two stack-column rows
  differ only in the argsort, and `%TypedArray%.sort` with a comparator is the
  difference between
  1.22x<!--m:column-kernels.stack-stack-columns.vsBase--> and
  2.31x<!--m:column-kernels.stack-stack-columns-cmp.vsBase-->.

Meet the first two and the encoder's payload is wiggle's 12 bytes a feature at
wiggle's speed, and the line, the density band and the whiskers band become
shape questions rather than byte questions. Leave them and the mark grammar
draws a thousand-source multi-wiggle at two thirds of wiggle's worker time while
shipping and holding two thirds more bytes than wiggle does, which is the trade
ADR-127 already declined once.
