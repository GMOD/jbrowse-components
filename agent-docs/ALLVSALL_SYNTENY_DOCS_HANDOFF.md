# All-vs-all / multiway synteny tutorial docs — handoff

_Date: 2026-07-10. Branch: `webgl-poc`._

## What was done (committed as `25fcd35ab1`, 4 files)

Improved `website/docs/tutorials/allvsall_synteny.md` + `multiway_synteny.md`:

- **Demo links are now spec-driven, not hand-coded.** Removed the intro inline
  links and the `<Figure link=…>` overrides (they pointed at `/main/` + a bare
  `?config=` with no session). Figures now auto-resolve to their
  `screenshot-specs` live link via `remark-figure.ts` — opening the exact
  captured session on `/latest/`. Rule saved to memory:
  demo/JBrowse-view links in docs must come from generate-screenshots specs.
- **Biology trimmed to one cited paragraph.** allvsall's "Backbone and islands"
  states the result + citations only: Hayashi et al. 2001 (Sakai backbone /
  S-loops / _stx_) and Welch et al. 2002 (three-strain 39.2%-shared,
  independently acquired virulence). Rewrote the stacked-figure caption to
  _prove_ backbone(=ribbons)-vs-islands(=gaps). multiway's direct-vs-transitive
  section trimmed 3 paragraphs → 1.
- **New one-vs-all figure** proves K-12's islands. Spec
  `multiway_synteny/ecoli_one_vs_all` in `scripts/specs/synteny.ts`: K-12 alone
  in a plain LGV, `LGVSyntenyDisplay`, whole chromosome (`loc: 'chr'`,
  `height: 200`, `viewportHeight: 420`). The all-vs-all track draws K-12 vs every
  other strain at once → continuous coverage = backbone, stark white gaps =
  K-12-specific islands. PNG generated and committed. (A 800 kb zoom was tried
  and rejected — coverage looked continuous there; whole-chromosome proves the
  islands far better.)

## Open item — CLOSED (`92109e41c7`)

**`website/src/lib/galleryLinks.generated.ts` committed, regenerated by the tool
and scoped to committed specs**, so `HEAD` is now self-consistent
(`gen:gallery-links-check` passes against committed specs) without sweeping other
agents' in-flight regenerations.

`galleryLinks.generated.ts` is an auto-generated file — never hand-edit it. To
get generator output scoped to *committed* specs (the generator otherwise reads
working-tree specs and folds in other agents' uncommitted edits):

- `git worktree add --detach <tmp> HEAD`, symlink `node_modules` from the main
  tree, run `pnpm gen:gallery-links` there, copy the result back, commit that one
  file with a pathspec, then restore the full working-tree copy from a backup
  (it still carries the other agents' uncommitted entries and stays `M`).
- This surfaced a bonus fix: committed `alignments.ts` rnaseq specs
  (`pkm_mutually_exclusive` + `strand_specific`, dropped `single_read`) had been
  committed earlier without regenerating galleryLinks, so `HEAD` was already
  stale. The regen folds those in too. Net commit diff: +ecoli_one_vs_all,
  +pkm_mutually_exclusive, +strand_specific, −single_read.
- The other agents' `scripts/specs/*` (methylation/popgen/sv/ui/features) +
  their galleryLinks entries remain theirs to commit when they land their work.

## Nice-to-have (not blocking, not CI-gated)

- `pnpm audit-figures` to refresh `scripts/figure-manifest.json`: its `liveUrl`
  for `ecoli_pangenome` / `ecoli_import_form` still shows the old hand-coded
  `/main/` URL, and the new `ecoli_one_vs_all` figure isn't listed. Left alone
  because it was mid-edit by another agent and isn't CI-checked.
- The "no hand-coded demo links" rule could be applied more broadly: other
  tutorials still hand-code `jbrowse.org/code/jb2/...` links (bxd_qtl,
  protein_structure, synteny_visualization, chromhmm, sv_visualization_cgiab,
  sv_multisamples, analyze_trio). Many are legitimate inline `session=spec-{…}`
  links, so audit case-by-case rather than blanket-stripping.

## Shared-worktree notes

Multiple agents active on this branch. My commit used a pathspec commit
(`git commit -m … -- <4 paths>`) to avoid the then-dirty shared index; verified
it contains exactly my 4 files. Do the same (never bare `git commit`, never
`git add -A`) until the tree quiets down.
